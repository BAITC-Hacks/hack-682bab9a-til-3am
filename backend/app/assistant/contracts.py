"""Canonical contracts shared by backend and the AI-agent module.

The definitions live in ``app.contracts`` for backwards compatibility with
the first backend slice. This module is the stable import path for the agent:
``app.assistant.contracts``.
"""

from app.contracts import *

